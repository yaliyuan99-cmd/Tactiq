# Public routes are prerendered to real HTML files, so Netlify serves them
# directly. Only the client-only (auth-gated or redirecting) routes need the
# SPA shell, and everything unknown gets a genuine 404.
/account      /index.html   200
/customize    /index.html   200
/admin        /index.html   200
/checkout     /index.html   200
/showcase     /             301
/*            /404.html     404
